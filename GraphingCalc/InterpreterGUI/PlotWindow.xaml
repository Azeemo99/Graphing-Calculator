<Window x:Class="InterpreterGUI.PlotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Function Plotter" Height="500" Width="800">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top controls -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 10">
            <TextBox x:Name="FunctionBox" Width="300" Text="Enter an expression" Margin="0,0,10,0"/>
            <Button x:Name="DrawButton" Content="Draw" Width="100" Click="DrawButton_Click"/>
            <TextBox x:Name="xStartBox" Text="Start x" Width="150" Margin="20,0,10,0"/>
            <TextBox x:Name="xEndBox" Text="End x" Width="150" Margin="10,0,20,0"/>
        </StackPanel>

        <!-- Differentiation controls -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 0 0 10">
            <Button x:Name="DiffButton" Content="Differentiate" Click="DiffButton_click" Margin="0,0,10,0"/>
            <TextBox x:Name="ResultBox" Text="Result" Width="300" IsReadOnly="True"/>
        </StackPanel>

        <!-- Plot canvas -->
        <Canvas x:Name="PlotCanvas" Grid.Row="2" Background="White" />
    </Grid>
</Window>
